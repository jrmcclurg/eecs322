;; This prints the initial ESP value
;; use the GC_DEBUG option in runtime.c
;; to check that this value is equal
;; to the value found by runtime.c
(
  (
    (ecx <- esp)
    (ecx *= 2)
    (ecx += 1)
    (eax <- (print ecx))
  )
)
